{
  "version": 2,
  "buildCommand": "cd frontend && npm install --legacy-peer-deps --force && npm run vercel-build",
  "outputDirectory": "frontend/build",
  "framework": "create-react-app",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
